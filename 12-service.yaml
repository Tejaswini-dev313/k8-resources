apiVersion: v1
kind: service
metadata:
  name: nginx
spec:
  selector:
    project: expense
    module: backend
    environment: dev
  Ports:
  - name: nginx-svc-Ports
    protocol: TCP
    port: 80 # service port
    targetport: 80 # container port