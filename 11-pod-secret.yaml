kind: Pod
apiVersion: v1
metadata:
  name: pod-secret
  lables:
    project: expense
    module: backend
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - secretRef:
        name: pod-secret 
