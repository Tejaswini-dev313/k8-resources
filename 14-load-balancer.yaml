apiVersion: v1
kind: service
metadata:
  name: nginx-la
sec:
  type: LoadBalancer
  selector:
    project: expense
    module: backend
    environment: dev
  ports:
  - name: nginx-svc-port
    protocol: TCP
    port: 80 # service port
    targetPort: 80 # container port